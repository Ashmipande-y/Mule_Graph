# Optional dependencies for running the isolated GraphSAGE benchmark.
# Core MuleGraph (FastAPI, ml/rules, canonical demo) does NOT require these.
torch>=2.0.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
